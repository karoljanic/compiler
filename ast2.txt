PROGRAM
    PROC test( ArgsDeclaration: (var a, ) )
        var b
        var c
        ASSIGN var c := num 0
        WHILE var c < num 5 DO
            IF var c == num 0
            THEN
                ASSIGN var b := num 5
            ELSE
                WRITE num 1
            ENDIF

            ASSIGN var c := var c + num 1
        ENDWHILE

        ASSIGN var a := var b
    ENDPROC
    MAIN
        var a
        var test#b
        var test#c
        ASSIGN var test#c := num 0
        WHILE var test#c < num 5 DO
            IF var test#c == num 0
            THEN
                ASSIGN var test#b := num 5
            ELSE
                WRITE num 1
            ENDIF

            ASSIGN var test#c := var test#c + num 1
        ENDWHILE

        ASSIGN var a := var test#b
        WRITE var a
    ENDMAIN
ENDPROGRAM
