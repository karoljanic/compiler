PROGRAM
    PROC test( ArgsDeclaration: (var a, ) )
        var b
        var c
        ASSIGN var c := num 0
        WHILE var c < num 5 DO
            IF var c == num 0
            THEN
                ASSIGN var b := num 5
            ELSE
                WRITE num 1
            ENDIF

            ASSIGN var c := var c + num 1
        ENDWHILE

        ASSIGN var a := var b
    ENDPROC
    MAIN
        var a
        CALL test(ArgsList: (var a, ))
        WRITE var a
    ENDMAIN
ENDPROGRAM
