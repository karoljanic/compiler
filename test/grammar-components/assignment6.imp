PROGRAM IS
    a, b[10], c[19], d[101], e
IN
    e := 0;
    a := 1+2;
    a := e * e;
    a := b[1] / d[2];
    a := b[a] % d[e];
    a := 1+e;
    a := e % c[1];
    a := d[e] / 2;
    
    d[e] := 1+2;
    d[e] := e * e;
    d[e] := b[1] / d[2];
    d[e] := b[a] % d[e];
    d[e] := 1+e;
    d[e] := e % c[1];
    d[e] := d[e] / 2;

    d[6] := 1+2;
    d[6] := e * e;
    d[6] := b[1] / d[2];
    d[6] := b[a] % d[e];
    d[6] := 1+e;
    d[6] := e % c[1];
    d[6] := d[e] / 2;

    c[d[2]] := 1+2;
END